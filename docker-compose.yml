services:
  rss-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rss-bot
    environment:
      RSS_INTERVAL: 120
    volumes:
      - ./config.yaml:/app/config.yaml:ro
#       - cache:/tmp/rss_cache
# volumes:
#   cache:
